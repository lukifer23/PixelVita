package com.example.androiddiffusion;

import org.junit.Test;
import static org.junit.Assert.*;

/**
 * This is a simple test class to verify that the Java configuration in VSCode is working correctly.
 */
public class JavaConfigTest {
    
    @Test
    public void testJavaConfig() {
        // This test doesn't do anything meaningful, it's just to verify that Java configuration works
        assertTrue("Java configuration is working", true);
    }
} 